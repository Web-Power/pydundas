# create env with:
# conda env create -p ./csa --file ./conda-env.yaml
# delete with:
# conda env remove -p ./csa
# On your terminal: conda activate -p ./csa
# csa = conda subject analysis
name: dundas_session
channels:
  - defaults
dependencies:
  - pyyaml
# Dependencies in *requirements.txt to be virtual env friendly, but it looks like the dependencies array cannot be empty.
